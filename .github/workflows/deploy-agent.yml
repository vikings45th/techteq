name: Deploy Agent to Cloud Run

on:
  pull_request:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # ① Checkout
      - name: Checkout
        uses: actions/checkout@v3

      # ② GCP 認証（ここが最重要）
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      # ③ gcloud CLI セットアップ
      - name: Set up gcloud
        uses: google-github-actions/setup-gcloud@v3
        with:
          project_id: firstdown-482704

      # ④ Docker 認証（Artifact Registry）
      - name: Configure Docker
        run: |
          gcloud auth configure-docker asia-northeast1-docker.pkg.dev --quiet

      # ⑤ Docker build
      - name: Build Docker image
        run: |
          docker build \
            -t asia-northeast1-docker.pkg.dev/firstdown-482704/agent-repo/agent:${{ github.sha }} \
            ./ml/agent

      # ⑥ Docker push
      - name: Push Docker image
        run: |
          docker push \
            asia-northeast1-docker.pkg.dev/firstdown-482704/agent-repo/agent:${{ github.sha }}

      # ⑦ Cloud Run deploy
      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy agent \
            --image asia-northeast1-docker.pkg.dev/firstdown-482704/agent-repo/agent:${{ github.sha }} \
            --region asia-northeast1 \
            --platform managed \
            --allow-unauthenticated
