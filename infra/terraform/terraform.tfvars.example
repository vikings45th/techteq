# コピーして terraform.tfvars にリネームし、値を編集してください。
# terraform.tfvars は .gitignore に含めることを推奨します。

project_id = "firstdown-482704"
region     = "asia-northeast1"

# 必要に応じて上書き
# bq_dataset_id          = "firstdown_mvp"
# artifact_registry_repos = ["agent-repo", "ranker-repo"]
# agent_runtime_sa_name  = "agent-runtime-sa"

# 既に手動で agent-runtime-sa がある場合は false（IAM のみ Terraform で付与）
# create_agent_runtime_sa = false

# --- Cloud Run（変更したいものだけここで上書き。サービス名・URL・キー・バージョンなど）
# agent_service_name = "agent"
# ranker_service_name = "ranker"
# Agent の run.invoker を付与する SA（Web を Cloud Run で動かす場合は Web の Cloud Run 用 SA のメール）。空なら Terraform で invoker を付与しない
# agent_invoker_sa_email = "123456789-compute@developer.gserviceaccount.com"
# Agent 利用時は Ranker の URL を必ず設定（他環境では自分の Cloud Run URL に変更）
# ranker_service_url = "https://ranker-203786374782.asia-northeast1.run.app"
# agent_image  = "asia-northeast1-docker.pkg.dev/PROJECT_ID/agent-repo/agent:latest"
# ranker_image = "asia-northeast1-docker.pkg.dev/PROJECT_ID/ranker-repo/ranker:latest"
# agent_env_features_version = "mvp_v1"
# agent_env_vertex_text_model = "gemini-1.5-flash-002"
# ranker_env_model_version   = "shadow_xgb_18feat"
# ranker_env_ranker_version  = "rule_v1"
# Vertex AI で推論する場合（18特徴量モデルを Vertex にデプロイ済みなら）
# ranker_env_model_inference_mode = "vertex"
# ranker_env_vertex_endpoint_id   = "3135439925533474816"
# ranker_env_vertex_timeout_s     = "5.0"
# MAPS_API_KEY は CI で渡す想定。apply 時にここで渡す場合は sensitive のため取り扱い注意
# agent_env_maps_api_key = ""
